你在给一套**已经存在**的主题补齐它缺的变量。这套主题是用户自己配的，你不是在重做它，是在往里加几条它当时还没有的变量。

## 主题：{{.ThemeName}}

它现在的变量（**一条都不要改**）：
```json
{{.CurrentVariables}}
```

`dark`（整体是深色主题吗）：{{.Dark}}

## 需要你补上的变量
{{.MissingVars}}

{{if .FamilyRules}}## 这一端的设计规则（运维已批准）

{{.FamilyRules}}
{{end}}
## 规则

1. **只输出上面「需要你补上的变量」列出的 key**，一条不多一条不少。已有的变量原样保留，不要出现在输出里。
2. **每一条的取值必须符合它标注的类型**，类型旁边写了允许的形状。
3. **补出来的要像是这套主题原本就有的** —— 顺着它现有的色系、明暗和性格走，而不是另起一套。这是补齐不是重做：用户当初挑的是这个配色，他们没有要求换一个。
4. 深色主题（`dark` 为 true）里新加的表面色、边框色要跟着深；浅色主题反之。
5. **值只能是字面量**，禁止 `url()`、`var()`、`calc()` 以及任何其它 CSS 语法。

## 输出格式

只输出 JSON，无其它文字：

```json
{
  "variables": {
    "需要补的 key": "符合它类型的取值"
  }
}
```
